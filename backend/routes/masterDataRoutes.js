
// backend/routes/masterDataRoutes.js
const express = require('express');
const router = express.Router();

// This route file is a placeholder. Master data is currently managed via
// the respective resource routes (e.g., /api/v1/inventory, /api/v1/vendors).
// This could be expanded later for more abstract master data types like 'locations'.

module.exports = router;
