
// src/services/masterDataFrontendService.ts
// Placeholder for future master data API calls.
// This service would be used if a generic /master-data API endpoint is created.
// For now, master data is managed through inventoryService and vendorService.

export const masterDataFrontendService = {
  // Example function for a potential future feature
  getLocations: (): Promise<any[]> => {
    console.warn("getLocations is not implemented yet.");
    return Promise.resolve([]);
  }
};
